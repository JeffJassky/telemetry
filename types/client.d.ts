/**
 * Client entry — placeholder. The real typed client (buffering, offline queue,
 * identify, trace context) lands with instrumentation stage 4b task 3; until
 * then the runtime export throws on call.
 */
export declare function createClient(): never;
