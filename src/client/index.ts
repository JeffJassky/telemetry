/**
 * Client entry — Stage 4b task 3 (instrumentation.md §6–7) builds the real
 * buffering/offline/identify client. This stub exists so the `/client` export
 * map entry resolves during server-core development; it must be replaced
 * before any publish.
 */
export function createClient(): never {
  throw new Error(
    '@jeffjassky/telemetry/client is not implemented yet — the server core shipped first. ' +
    'Track progress in plans/build-plan.md §13.',
  );
}
